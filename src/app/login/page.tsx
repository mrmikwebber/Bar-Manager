"use client";import {useState} from "react";import {createClient} from "@/lib/supabase/client";
export default function Login(){const[email,setEmail]=useState("");const[msg,setMsg]=useState("");async function send(){const{error}=await createClient().auth.signInWithOtp({email,options:{emailRedirectTo:location.origin}});setMsg(error?error.message:"Check your email for the sign-in link.")}return <main className="pt-16"><div className="eyebrow">Bar Manager</div><h1 className="mt-2 text-3xl font-semibold">Your bar, everywhere.</h1><p className="muted mt-3">Sign in with a magic link. No password to remember.</p><input className="input mt-8" type="email" placeholder="you@example.com" value={email} onChange={e=>setEmail(e.target.value)}/><button className="primary mt-3 w-full" onClick={send}>Email me a sign-in link</button>{msg&&<p className="muted mt-4 text-sm">{msg}</p>}</main>}