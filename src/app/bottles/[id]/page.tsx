import {demoBottles} from "@/lib/catalog/demo";import Link from "next/link";
export default async function Bottle({params}:{params:Promise<{id:string}>}){const{id}=await params;const b=demoBottles.find(x=>x.id===id)??demoBottles[0];return <main><Link href="/" className="muted text-sm">‹ My Bar</Link><div className="mt-7 eyebrow">{b.type}</div><h1 className="mt-1 text-3xl font-semibold">{b.name}</h1><div className="muted mt-2">{b.abv}% ABV</div><div className="card mt-7 p-5"><div className="text-sm font-semibold">Flavor profile</div><div className="mt-4 flex flex-wrap gap-2">{b.flavors.map((f,i)=><span className="pill" key={f}>{f} · {Math.max(55,92-i*11)}%</span>)}</div></div><div className="card mt-3 p-5"><div className="text-sm font-semibold">Use it as</div><p className="muted mt-2 text-sm">Modifier · accent · flavor direction</p></div><Link href={"/workbench?bottle="+b.id} className="primary mt-5 block text-center">Build with this bottle</Link></main>}