"use client";import {useState} from "react";import {demoBottles} from "@/lib/catalog/demo";
export default function Catalog(){const[q,setQ]=useState("");const shown=demoBottles.filter(b=>b.name.toLowerCase().includes(q.toLowerCase())||b.type.toLowerCase().includes(q.toLowerCase()));return <main><div className="eyebrow">Catalog</div><h1 className="mt-1 text-3xl font-semibold">Add a bottle</h1><p className="muted mt-2 text-sm">Search the shared bottle catalog, then add what you own.</p><input autoFocus value={q} onChange={e=>setQ(e.target.value)} className="input mt-6" placeholder="Cassis, mezcal, Cointreau…"/><div className="mt-4 grid gap-3">{shown.map(b=><div className="card p-4" key={b.id}><div className="row"><div><div className="font-semibold">{b.name}</div><div className="muted mt-1 text-sm">{b.type} · {b.abv}%</div></div><button className="primary">Add</button></div></div>)}</div><div className="card mt-4 p-4"><div className="font-medium">Can't find it?</div><p className="muted mt-1 text-sm">Missing searches can become enrichment jobs so the catalog grows from actual usage.</p></div></main>}